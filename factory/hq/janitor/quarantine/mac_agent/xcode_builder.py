"""Xcode builder — handles build, archive, export operations."""
# The build logic is embedded in mac_build_agent.py for simplicity.
# This module exists for future extraction if the agent grows.

# See mac_build_agent.py: _build_ios(), _archive(), _run_tests()
