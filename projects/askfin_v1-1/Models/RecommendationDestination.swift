// Minimum viable routing — expand as navigation layer is built
enum RecommendationDestination {
    case practiceCategory(id: String)
    case examSimulation
    case reviewMistakes
    case streakScreen
}