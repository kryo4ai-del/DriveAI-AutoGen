/**
 * Submitted answer awaiting validation
 */
export interface SubmittedAnswer {
  questionId: string;
  selectedAnswerId: string;
  submittedAt: number; // timestamp for analytics
}

/**
 * Active exam session
 */