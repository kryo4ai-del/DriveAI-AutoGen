/**
 * Client-side wrapper for SkillMap.
 * Suspense boundary handles async data fetching in useSkillData.
 * @requires 'use client' in parent or direct client component usage
 */
export function SkillMap(props: SkillMapProps) {
  // ...
}