// In ExperimentMetrics.init - calculations mixed with model
struct ExperimentMetrics: Codable {
    init(variantId: String, events: [ExperimentEvent]) {
        // Correctness calculation
        // Timing calculation
        // Engagement calculation
        // All inline
    }
}