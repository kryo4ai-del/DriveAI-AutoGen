# specs/

Feature-Spec-Dateien für den DriveAI-AutoGen Swarm.

## Erstellt von
Prompt Engineer Agent (`~/.claude/agents/prompt-engineer.md`)

## Format
- Dateiname: `snake_case` des AutoGen-Namens
- Inhalt: Goal, Requirements, Design Constraints, CLI Command
- Schema: siehe `~/.claude/agents/prompt-engineer.md`

## Verwendung
```bash
python main.py --spec specs/<feature_name>.md --profile dev --approval auto
```

## Dateien
| Datei | Feature | Status |
|-------|---------|--------|
| _(noch keine)_ | | |